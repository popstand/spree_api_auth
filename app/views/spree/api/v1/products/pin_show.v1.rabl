object @pin
node(:product_name)   { |p| p.product_name }
node(:brand_name)     { |p| p.brand_name }
node(:original_price) { |p| p.original_price }
node(:sale_price)     { |p| p.sale_price }
node(:image_url)      { |p| p.image_url }
node(:product_url)    { |p| p.product_url }
node(:board_id)       { |p| p.board_id }
